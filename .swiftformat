# SwiftFormat Configuration for LexiconFlow

# Swift version
--swift-version 6.0

# Basic formatting
--indent 4
--wrap-arguments before-first
--wrap-collections before-first

# Explicit self - insert for Swift 6 strict concurrency compatibility
# Swift 6 requires explicit self in Sendable closures, so we always insert it
--self insert

# Empty braces
--empty-braces no-space

# Import organization
--import-grouping testable-last

# Decimal grouping
--decimal-grouping none

# Trailing commas - disabled to match SwiftLint configuration
--trailing-commas never
