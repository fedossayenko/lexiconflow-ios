# SwiftFormat Configuration for LexiconFlow

# Swift version
--swift-version 6.0

# Basic formatting
--indent 4
--wrap-arguments before-first
--wrap-collections before-first

# Explicit self - insert explicit self for Swift 6 strict concurrency
# Swift 6 requires explicit self. in closure capture semantics (@MainActor classes)
--self insert

# Empty braces
--empty-braces no-space

# Import organization
--import-grouping testable-last

# Combine imports - only import when actually using Combine types
# @Published is provided by SwiftUI, so import Combine is NOT needed
# unless using Publishers, Subscribers, Subjects, etc.

# Decimal grouping - explicitly ignore to override CI defaults
--decimalgrouping ignore

# Trailing commas - disabled to match SwiftLint configuration
--trailing-commas never
